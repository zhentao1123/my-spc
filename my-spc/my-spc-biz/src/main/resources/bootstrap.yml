spring:
  application:
    name: biz-service
    
  cloud:
    config:
      fail-fast: true
      label: master
      #uri: http://${CONFIG_SERVER_HOST}:${CONFIG_SERVER_PORT}
      uri: http://127.0.0.1:8011
      
management:
  endpoints:
    web:
      exposure:
        include: '*'